# Tmux Terminals

一键恢复 Remote-SSH 断开后的终端工作区。

## 解决什么问题

用 VS Code Remote-SSH 时，连接一断，所有集成终端标签连同里面正在
跑的进程一起消失。重连后要手工重建每个终端、重新 `cd` 到各个工程
目录、重新敲一遍启动命令。

这个扩展在侧边栏维护一张终端清单。点一下，终端就回来了；如果远端
的 tmux 会话还活着，连**正在跑的进程和滚动历史**也一起接回来。

## 三个概念

- **条目**：一条记录，含 名称 / 远程目录 / profile / 模型（可选）。名称只
  用于显示，随时可改；它**不是** tmux 会话名 —— 会话名由条目 id 派生，形如
  `tmuxterm-<id>`，所以改条目名称不会影响已建立的会话。
  条目**没有**自定义命令字段：启动命令完全由 `profile` 派生（见下）。
- **存活**：远端存在该条目对应的 tmux 会话。
- **接回**：附到已存活的会话上，进程和历史都还在。

## profile：直连 / 中转

每个条目带一个 profile，决定用哪个 claude 入口启动、从哪读模型清单：

| profile | 启动命令 | 模型清单来源 |
|---|---|---|
| `ccr`（本地中转，默认） | `claude --dangerously-skip-permissions` | `~/.claude/settings.json` |
| `direct`（官方直连） | `claude-direct --dangerously-skip-permissions` | `/etc/claude/direct.json` |

清单条目的徽标颜色表示 profile：🔵 蓝 = `ccr`，🟠 橙 = `direct`。
旧版（v1）清单迁移时，命令里含 `claude-direct` 的条目映射为 `direct`，
其余映射为 `ccr`。

## 安装

需要在**已连接到远端**的 VS Code 窗口里安装，扩展才会装到远端。

```bash
npx @vscode/vsce package
```

然后在远端窗口的扩展面板里选择「从 VSIX 安装」。

> 本扩展声明了 `extensionKind: workspace`，必须运行在远端才能调用
> 远端的 tmux。装在本地是无效的。

## 使用

| 操作 | 效果 |
|---|---|
| 标题栏 `+` | 新建条目（依次输入 名称 → 目录 → 是否参与全部恢复；profile 默认 `ccr`） |
| 点击条目 | 会话还活着 → 接回原进程（claude 还在跑就只接回，绝不打扰）；<br>claude 已退出 / 会话已死 → 重建并**接回这个条目自己那条对话** |
| 标题栏 ⟳ | 恢复所有标记了「参与全部恢复」的条目 |
| 标题栏 ↻ | 只重查存活状态，不开终端 |
| 右键条目 | 编辑 / 复制 / 切换自动恢复 / 设置模型 / 切换直连-中转 / **选择要接回的对话…** / 删除条目 / 杀掉远端会话 |

条目的启动命令不接受手输：它是 `profile` 的派生物（`ccr` → `claude`，
`direct` → `claude-direct`，若设了模型再带 `--model`）。要换启动方式就换
profile。

## 条目 ↔ 对话绑定

条目**跟随这个终端实际在跑的那条 claude 对话**：每次要接回之前先看一眼终端
（`pane_pid` → 后代里的 claude → 它自己的会话注册表），按观测结果把绑定刷过去，
再用 `claude --resume <id>` 精确接回，不需要手动 `/resume`，多个条目共用同一个
目录也不会串。所以你在终端里 `/new` 开了一条新对话，条目也会跟着改绑过去 ——
不会在下次恢复时把那条已经弃用的对话又接回来：

- 新建条目时即分配一个对话 id，**首次启动** → `claude --session-id <该 id>`
  把这条对话建出来；
- 之后的每次恢复 → `claude --resume <该 id>`（`--resume` 按 cwd 作用域，
  而会话本来就是用条目目录建的，天然满足）；
- **老条目（本功能上线前建的，还没有绑定）** → 第一次需要接回时弹一次选择框，
  候选是该目录下的历史对话（显示时间 / 首条消息摘要 / 体积），选中即永久记住；
  列表末尾的「**＋ 新建一条对话**」用于明确地开一条新的；按 `Esc` 取消则
  **这次不启动 claude**（终端照常可用），下次再问；
- 随时可用右键「**选择要接回的对话…**」重新绑定；条目 tooltip 里能看到当前
  绑定的对话。**手动选的这条不会被下一次观测冲掉** —— 手动改绑只改「接回
  哪条」，不动「上次观测到的活跃会话」，于是下一次观测看到的还是同一条，
  等于没变化。唯一的例外是**终端自己换了对话**（你在里面 `/new`，或
  `/resume` 到另一条）：那是一条新的观测事实，条目跟过去，手动选择让位；
- **不会静默丢关联**：绑定的对话记录不在时会明确提示「本次新开一条」；选中
  一条**已经绑给别的条目**的对话会弹确认（两个终端写同一条记录可能损坏它），
  不确认就不接；
- 会话还活着、claude 还在跑时**只接回、不发送任何东西**；claude 已退出才会
  用 `--resume` 接回它自己那条。接不上时会明确报错，而不是默默退回裸 shell。

## 侧边栏的层级：第三级「任务名」

侧边栏是三级：**文件夹（按目录分组）→ 条目 → 任务名**。第三级显示那个终端
当前在做的任务，名字来源按优先级：

1. **实时 pane 标题**（权威）：claude 运行时会把自己的任务名写进终端标题，
   扩展剥掉前面的状态指示符后取来做名字；
2. **绑定对话的 `aiTitle`**（回退）：claude 不在跑（或没写标题）时，从该条目
   绑定对话的记录（`~/.claude/projects/**/<对话 id>.jsonl`）里读 `ai-title` 行；
3. **两个都没有 → 不显示第三级。** 这是刻意的产品决定，不是待修的缺陷：
   宁可不显示，也不拿占位文字或目录名之类「看起来像名字」的东西顶上 ——
   把「不知道」伪装成「知道」会误导；回退来的名字也可能只是「上次那条对话
   的标题」，本来就不等于当前任务。

## 排序与批量操作

- **拖拽排序**：在「终端清单」里直接拖动条目即可重排。落点语义是「插到
  目标行之前」，拖到列表末尾 = 放到最后。排序只改本地清单顺序，**绝不
  触发任何 tmux 操作**，也不影响已建立的会话。
- **批量操作**：在「批量操作」面板里点击条目切换选中（实心勾 = 已选，
  空心圈 = 未选）。标题栏三个按钮把选中条目统一「设为中转 / 设为直连 /
  批量设置模型」。
- **短路径消歧**：目录以最短且可区分的形式显示 —— 从 basename 起，与
  他人重名就往上多带一层父目录，直到唯一。若多条条目共用**完全相同**的
  目录（无论怎么加长都无法唯一），则都显示 basename：同目录的条目本来就
  靠行标签（条目名）区分，描述不必也不能替它们分家。

## 重要：启动命令只在「新建会话」时执行

会话还活着时，点击是**接回**，由 profile 派生的启动命令**不会**执行 ——
你看到的是会话里原有的进程。

这是刻意的：如果往一个正在运行的会话里发命令，那行命令会作为键盘
输入进入你正在跑的进程（比如一个 claude、一个编译、一个 REPL），
后果不可预料。

想按 profile 重新启动一次，先右键「杀掉远端 tmux 会话」，再点击条目。

## 配置

| 配置项 | 默认 | 说明 |
|---|---|---|
| `tmuxTerminals.pollInterval` | `10000` | 存活状态轮询间隔（ms），`0` 关闭 |
| `tmuxTerminals.tmuxPath` | `tmux` | tmux 可执行文件路径 |
| `tmuxTerminals.storagePath` | `""` | 清单文件路径。留空用扩展存储目录，**重装扩展会清空** |

## 已知副作用：运行中切模型会改全局默认

- **未运行**的会话：切模型只改条目配置，下次打开时经 `--model` 启动参数
  带出，**不污染**全局默认。
- **运行中**的会话：切模型会发送 `/model <目标>` 立即生效（重启会丢对话，
  命令行 `--model` 也盖不过 resumed 会话，故必须用 `/model`）。这会**顺带
  改写** `~/.claude/settings.json` 里的全局默认模型。这是「立即生效」的
  代价，扩展如实照做、不试图规避 —— 若不想动全局默认，请在会话空闲时切
  或事后手动改回。
- **激活时会观测并可能改绑**：扩展激活（以及面板变可见、展开节点、点 ⟳）
  都会看一眼「这个终端在跑哪条对话」，绑定与观测不符就写回清单 —— 于是
  **重载一次窗口就会让清单文件（`terminals.json`，或 `storagePath` 指定的
  那个文件）的 mtime 变化**，哪怕你没有做任何操作。这是刻意的（激活是既定
  的触发点），但它与 `src/core/store.ts` 里「打开个扩展就改了用户文件」是
  不可接受的副作用这一原则**存在张力**，故在此显式说明。

## 已知限制

- 条目按远端机器隔离（扩展在每台远端各存一份清单）。
- 远端没装 tmux 时仍能开终端并 `cd`，但没有接回能力。
- 远端机器重启过则所有 tmux 会话消失，所有条目会显示为 ⚪。

## 开发

```bash
npm install
npm test          # 纯逻辑单测 + tmux 集成测试
npm run compile
```

按 F5 启动 Extension Development Host 调试。

## License

MIT
